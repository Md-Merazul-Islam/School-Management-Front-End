import React, { useState } from 'react';

const Tabs_show = () => {
  const [activeTab, setActiveTab] = useState('stats');

  const handleTabClick = (tab) => {
    setActiveTab(tab);
  };

  return (
   

  

  );
};

export default Tabs_show;
